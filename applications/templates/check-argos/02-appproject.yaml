---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: check-argos
spec:
  destinations:
    - namespace: '*'
      server: '*'
  sourceRepos:
    - https://github.com/conorfennell/optimizer-k8s.git
  destinations:
    - server: https://kubernetes.default.svc
      namespace: check-argos
  description: "Applications that are used for check argos"
